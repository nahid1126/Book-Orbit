package com.nahid.book_orbit.data.local

class Converters{

    /*@TypeConverter
    fun toQuestionHolder(holder: QuestionHolder): String {
        return Json.encodeToJsonElement(holder).toString()
    }

    @TypeConverter
    fun fromQuestionHolder(holder: String): QuestionHolder {
        return Json.decodeFromString(holder)
    }*/

}